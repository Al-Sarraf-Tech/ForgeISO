# Generated by haskell-ci-orchestrator — DO NOT EDIT MANUALLY
name: ForgeISO CI
on:
  push:
    tags:
      - v*.*.*
      - v*
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * 1'
permissions:
  contents: write
  id-token: write
  attestations: write
  security-events: write
concurrency:
  group: ForgeISO-ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  lint:
    name: Lint
    runs-on: [self-hosted, unified-all]
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Check formatting
        shell: bash
        timeout-minutes: 5
        run: cargo fmt --all --check
      - name: Clippy lints
        shell: bash
        timeout-minutes: 10
        run: cargo clippy --workspace --all-targets -- -D warnings
  test:
    name: Test
    runs-on: [self-hosted, unified-all]
    needs: lint
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cache ~/.cargo/registry
        uses: actions/cache@v4
        with:
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          path: ~/.cargo/registry
          restore-keys: ${{ runner.os }}-cargo-registry-

      - name: Cache ~/.cargo/git
        uses: actions/cache@v4
        with:
          key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}
          path: ~/.cargo/git
          restore-keys: ${{ runner.os }}-cargo-git-

      - name: Cache target
        uses: actions/cache@v4
        with:
          key: ${{ runner.os }}-cargo-target-${{ hashFiles('**/Cargo.lock') }}
          path: target
          restore-keys: ${{ runner.os }}-cargo-target-

      - name: Cargo test
        shell: bash
        timeout-minutes: 20
        run: cargo test --workspace
  security:
    name: Security
    runs-on: [self-hosted, unified-all]
    needs: lint
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Secrets scan (gitleaks)
        shell: bash
        timeout-minutes: 10
        run: gitleaks detect --source=. --no-banner --exit-code=1
      - name: Cargo audit
        shell: bash
        timeout-minutes: 10
        run: cargo audit
      - name: Cargo deny
        shell: bash
        timeout-minutes: 10
        run: cargo deny check bans licenses sources
      - name: ShellCheck
        shell: bash
        continue-on-error: true
        timeout-minutes: 10
        run: find . -name '*.sh' -type f | xargs shellcheck --severity=warning
      - name: Shell formatting (shfmt)
        shell: bash
        continue-on-error: true
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs shfmt -d
      - name: Trivy filesystem scan
        shell: bash
        timeout-minutes: 15
        run: trivy fs --severity HIGH,CRITICAL --exit-code 1 .
      - name: Syft SBOM generation
        shell: bash
        timeout-minutes: 10
        run: |
          syft dir:. -o spdx-json=sbom-spdx.json -o cyclonedx-json=sbom-cyclonedx.json
  sbom:
    name: SBOM
    runs-on: [self-hosted, unified-all]
    needs: test
    if: github.ref == 'refs/heads/main'
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Generate SBOM (syft)
        shell: bash
        timeout-minutes: 10
        run: |
          syft dir:. -o spdx-json=sbom-spdx.json -o cyclonedx-json=sbom-cyclonedx.json
      - name: Upload SBOM
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom-*.json
          retention-days: 90
  integration:
    name: Integration
    runs-on: [self-hosted, unified-all]
    needs: test
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: "Integration: rust-ci"
        shell: bash
        timeout-minutes: 30
        run: |
          echo 'Integration batch: rust-ci'
      - name: "Integration: sbom-audit"
        shell: bash
        timeout-minutes: 30
        run: |
          echo 'Integration batch: sbom-audit'
      - name: "Integration: gui"
        shell: bash
        timeout-minutes: 30
        run: |
          echo 'Integration batch: gui'
      - name: "Integration: security"
        shell: bash
        timeout-minutes: 30
        run: |
          echo 'Integration batch: security'
      - name: "Integration: integration"
        shell: bash
        timeout-minutes: 30
        run: |
          echo 'Integration batch: integration'
      - name: "Integration: e2e"
        shell: bash
        timeout-minutes: 30
        run: |
          echo 'Integration batch: e2e'
      - name: "Integration: lint"
        shell: bash
        timeout-minutes: 30
        run: |
          echo 'Integration batch: lint'
  release:
    name: Release
    runs-on: [self-hosted, unified-all]
    needs:
      - test
      - security
    if: startsWith(github.ref, 'refs/tags/')
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Generate checksums
        shell: bash
        timeout-minutes: 5
        run: cd release-assets && sha256sum * > SHA256SUMS
      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: release-assets/*
      - name: Create GitHub Release
        uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: release-assets/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
